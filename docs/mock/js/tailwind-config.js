/* Tailwind カスタムテーマ設定（CDN利用時は各HTMLで読み込み） */
tailwind.config = {
    theme: {
        extend: {
            colors: {
                main: '#003b82',
                subBlue: '#007cba',
                lightBlue: '#8ed1fc',
                cream: '#fff5cb',
                neutral: '#abb8c3',
            }
        }
    }
};
