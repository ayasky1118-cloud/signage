# Amplify 手動デプロイ手順

GitLab CI の Runner が利用できない場合、ローカルから Amplify に直接デプロイする手順。

---

## 前提

- Node.js / npm がインストール済み
- AWS CLI がインストール・設定済み（`aws configure` または環境変数で認証情報を設定）
- Amplify アプリへのデプロイ権限がある IAM ユーザー/ロール

---

## 手順

### 方法 1: スクリプトで一括実行（推奨）

```bash
cd /Users/yoshimi/dev/signage
chmod +x scripts/deploy-amplify.sh
./scripts/deploy-amplify.sh
```

### 方法 2: 手動で実行

```bash
cd /Users/yoshimi/dev/signage/frontend

# 1. ビルド（検証環境用。.env.staging の VITE_API_BASE を使用）
npm ci
npm run build:staging

# 2. ZIP 作成
cd dist
zip -r ../build.zip .
cd ..

# 3. デプロイ
aws amplify create-deployment \
  --app-id d27nzen1vhtcee \
  --branch-name main \
  --region ap-northeast-1 \
  --output json > deploy_info.json

UPLOAD_URL=$(python3 -c "import json; print(json.load(open('deploy_info.json'))['zipUploadUrl'])")
JOB_ID=$(python3 -c "import json; print(json.load(open('deploy_info.json'))['jobId'])")

curl -X PUT --upload-file build.zip "$UPLOAD_URL"

aws amplify start-deployment \
  --app-id d27nzen1vhtcee \
  --branch-name main \
  --job-id "$JOB_ID" \
  --region ap-northeast-1
```

---

## 環境変数について

現在構築済みの AWS は**検証環境**。スクリプトはデフォルトで `npm run build:staging` を実行し、`frontend/.env.staging` の `VITE_API_BASE` を使用する。

- **検証デプロイ**（デフォルト）: `./scripts/deploy-amplify.sh` → `.env.staging`
- **本番デプロイ**（本番 AWS 構築後）: `DEPLOY_ENV=production ./scripts/deploy-amplify.sh` → `.env.production`

Cognito や MapTiler など、その他の環境変数は `frontend/.env` に設定（全環境共通）。

---

## デプロイ確認

- **Amplify URL**: https://main.d27nzen1vhtcee.amplifyapp.com
- デプロイ完了まで数分かかることがあります
- ブラウザで強制リロード（Cmd+Shift+R）してキャッシュをクリア
