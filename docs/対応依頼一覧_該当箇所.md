# 対応依頼一覧と該当箇所

チャットグループ [e19883cf-6d92-4306-8574-d48f6511ce2d] のやり取りから、対応依頼と該当ソース箇所を抽出した一覧です。

---

## 1. DB・スキーマ関連

| 依頼内容 | 該当箇所 |
|----------|----------|
| html_object / html_object_value テーブル作成 | `db/00_schema/011_html_object.sql`, `012_html_object_value.sql` |
| display_label, ui_component 追加→削除 | 上記 DDL、`docs/テーブル定義/11_html_object.md`, `12_html_object_value.md` |
| value_data を画像配置対応、sample_image_path 追加 | `012_html_object_value.sql`, `docs/テーブル定義/12_html_object_value.md` |
| ルート描画・画像配置のサンプル画像（PNG）作成・DB投入 | `frontend/public/samples/route/`, `frontend/public/samples/image/` |
| 画像配置：信号機・学校・警察・役所 | 上記 image/、DB の html_object_value |
| ドキュメント修正（public/samples、画像配置の例） | `docs/201.ディレクトリ構成.md`, `docs/テーブル定義/12_html_object_value.md` |

---

## 2. 全画面編集サイドバー（OrderDetail.vue）

| 依頼内容 | 該当箇所 |
|----------|----------|
| ルート描画等のラベル・項目をDB取得値で配置 | `OrderDetail.vue` 全画面編集サイドバー、`backend/app/routers/html_object.py`, `useHtmlObjectApi.ts` |
| ドロップダウン｜選択｜描画 の3要素横並び | `OrderDetail.vue` 1602行付近 |
| 選択ボタンで画像+説明のモーダル表示 | `HtmlObjectValueSelectModal.vue`, `OrderDetail.vue` |
| 選択ボタンのスタイルを他ページに合わせる | `OrderDetail.vue` 選択ボタン（h-[2.25rem] w-[2.25rem]） |
| 画像配置の描画ボタン→「配置」、区分ごとのアイコン | `OrderDetail.vue` ACTION_LABEL_BY_CATEGORY、描画/配置ボタン |
| ルート描画：鉛筆アイコン、画像/テキスト/吹き出し：マップピンアイコン | `OrderDetail.vue` 1664-1684行付近 |
| モーダルヘッダーの白い部分の修正 | `HtmlObjectValueSelectModal.vue`（card-header-full, border-b） |
| 選択した画像をドロップダウン下に表示 | `OrderDetail.vue` 1685-1694行付近 |
| 画像・説明を横並び、小さい画像（h-8 w-8） | 上記と同じブロック |
| ルート・画像ドロップダウンに「選択してください」初期値 | `OrderDetail.vue` SELECT_PLACEHOLDER_VALUE, option, selectedValueIdByObjectId |
| 新規枝番追加時・全画面編集を開いた時の初期化 | `OrderDetail.vue` openFullscreenEdit, 枝番追加時の初期化処理 |
| テキスト後ろに配置ボタン | `OrderDetail.vue` テキスト配置・吹き出し配置の入力欄直後 |
| 吹き出しの「追加」→「配置」に変更 | `OrderDetail.vue` ACTION_LABEL_BY_CATEGORY BALLOON_PLACEMENT |
| テキスト追加モード・吹き出し追加モードボタン削除 | `OrderDetail.vue`（該当ボタンを削除） |

---

## 3. ツールチップ（ⓘ）

| 依頼内容 | 該当箇所 |
|----------|----------|
| ネイティブ title → カスタムツールチップ | `OrderDetail.vue` 1610-1620行付近 |
| ツールチップの背景・文字色（薄いグレー・濃いグレー） | 上記 span の class（bg-slate-100, text-slate-800） |
| ルートの？メッセージを下に→**元に戻す**（アイコン上） | `OrderDetail.vue` bottom-full, mb-1（1643行付近） |

---

## 4. 注文一覧（OrderList.vue）

| 依頼内容 | 該当箇所 |
|----------|----------|
| ダブルクリックで詳細画面が表示されます のインフォメーション | `OrderList.vue` 一覧テーブル上 |
| インフォメーションマーク（ⓘ）を配置 | 上記の前 |
| インフォメーションを一覧テーブルカードの直上に配置 | `OrderList.vue` |
| 左余白を狭く（pl-6→pl-3） | `OrderList.vue` インフォメーションの padding |
| 検索ボタンを虫眼鏡アイコンに | `OrderList.vue`, `OrderDetail.vue`, `OrderMain.vue` |
| 検索文言を消す | 同上 |
| ボタン幅を選択ボタンに合わせる | 同上（h-[2.25rem] w-[2.25rem]） |

---

## 5. ページ遷移・スクロール

| 依頼内容 | 該当箇所 |
|----------|----------|
| ルート遷移時に overflow リセット | `App.vue` router.afterEach |
| OrderDetail アンマウント時に overflow リセット | `OrderDetail.vue` onBeforeUnmount |

---

## 6. マッププレビュー（最大化時の表示）

| 依頼内容 | 該当箇所 |
|----------|----------|
| レイアウト/マッププレビュー：表示のみ。編集は全画面で | `MapPreview.vue` interactive プロパティ、`OrderDetail.vue` :interactive="false" |
| 全画面編集用マップ：パン・ズーム・編集操作可能 | `OrderDetail.vue` 全画面編集用 MapPreview（interactive はデフォルト true） |

---

## 7. テンプレートダミー画像

| 依頼内容 | 該当箇所 |
|----------|----------|
| モックのテンプレートダミー画像をテストデータとして使用 | `frontend/public/samples/template/template-dummy.png`, `docs/mock/assets/template-dummy.png` |
| サンプルイメージを工事案内標識に差し替え | 上記ファイルを画像で置換 |
| 変わっていません（キャッシュバスティング） | `?v=2` を TemplateSelectModal.vue, OrderDetail.vue, OrderMain.vue, order_main.html, order_detail.html に追加 |
| ディレクトリ構成ドキュメント | `docs/201.ディレクトリ構成.md` public/samples/template/ の説明 |

---

## 8. 看板情報ブロック（OrderDetail.vue）

| 依頼内容 | 該当箇所 |
|----------|----------|
| 枝番タブ・「枝番情報がありません…」と追加・削除ボタンを同一行に | `OrderDetail.vue` 1365-1435行付近（flex justify-between） |

---

## ファイル別の該当箇所まとめ

| ファイル | 主な対応内容 |
|----------|--------------|
| `frontend/src/pages/OrderDetail.vue` | 全画面編集サイドバー、枝番タブ、ツールチップ、テンプレートプレビュー、検索ボタン、overflow リセット |
| `frontend/src/pages/OrderList.vue` | インフォメーション、検索ボタン |
| `frontend/src/pages/OrderMain.vue` | 検索ボタン、テンプレートプレビュー |
| `frontend/src/components/MapPreview.vue` | interactive プロパティ |
| `frontend/src/components/TemplateSelectModal.vue` | テンプレートダミー画像 |
| `frontend/src/components/HtmlObjectValueSelectModal.vue` | 画像+説明選択モーダル、ヘッダー表示 |
| `frontend/src/App.vue` | ルート変更時の overflow リセット |
| `frontend/src/composables/useHtmlObjectApi.ts` | HTMLオブジェクトAPI取得 |
| `backend/app/routers/html_object.py` | GET /html-objects |
| `db/00_schema/011_html_object.sql`, `012_html_object_value.sql` | テーブル定義 |
| `docs/201.ディレクトリ構成.md` | public/samples、html_object 等の説明 |
| `docs/mock/order_main.html`, `order_detail.html` | テンプレートプレビュー、キャッシュバスティング |
